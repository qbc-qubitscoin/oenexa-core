server:
  port: 8089
spring:
  application:
    name: oenexa-notification-service
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/notification_db?createDatabaseIfNotExist=true
    username: root
    password: ${MYSQL_PASSWORD:verysecret}
  kafka:
    bootstrap-servers: localhost:9092
  mail:
    host: localhost
    port: 25
